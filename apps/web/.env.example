# Frontend environment example
# Copy to .env (or .env.local) and adjust.

# Base URL for the API (must include /api suffix)
VITE_API_URL=http://localhost:3000/api

# Socket.io origin (omit to derive from VITE_API_URL by stripping /api)
# VITE_SOCKET_URL=http://localhost:3000

# Custom application title (used in UI headers / tab title)
VITE_APP_TITLE=AI Interview Assistant (Local)
